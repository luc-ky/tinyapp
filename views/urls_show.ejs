<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
  <head>
    <%- include('partials/_head') %>
    <title>TinyApp - URL Details</title>
  </head>
  <body>
    <%- include('partials/_header') %>
    <div class="card text-center">
      <div class="card-header"></div>
      <div class="card-body">
        <h5 class="card-title">TinyURL for: <%= longURL %></h5>
        <p class="card-text">
          Short URL ID: <a href="/u/<%= id %>"><%= id %></a>
        </p>
      </div>
      <div class="card-footer text-muted">
        <h5>Edit</h5>
        <form action="/urls/<%= id %>?_method=PUT" method="POST">
        <label for="newURL">New URL:</label>
        <input type="text" name="newURL" value="<%= longURL %>" style="width: 300px; margin: 1em">
        <button type="submit" class="btn btn-primary">Update</button>
        </form>
      </div>
    </div>
    <%- include('partials/_footer') %>
  </body>
</html>